apiVersion: v1
kind: Service
metadata:
  name: member
  labels:
    app: member
spec:
  type: {{ .Values.service.type }}
  selector:
    app: member
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
