apiVersion: v1
kind: ConfigMap
metadata:
  name: member-config
data:
  {{- if eq .Values.secretBackend "vault" }}
  # dev 전용: Authentik issuer URI, DB URL, Redis host/port, IdP 클라이언트 정보
  SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI: {{ .Values.config.authIssuerUri | quote }}
  SPRING_DATASOURCE_URL: "jdbc:mysql://{{ .Values.config.dbHost }}:{{ .Values.config.dbPort }}/{{ .Values.config.dbName }}?serverTimezone=UTC&characterEncoding=UTF-8"
  SPRING_DATA_REDIS_HOST: {{ .Values.config.redisHost | quote }}
  SPRING_DATA_REDIS_PORT: "6379"
  AUTH_IDP_TOKEN_URI: {{ .Values.config.authTokenUri | quote }}
  AUTH_IDP_CLIENT_ID: {{ .Values.config.authClientId | quote }}
  AUTH_IDP_API_BASE_URI: {{ .Values.config.authApiBaseUri | quote }}
  {{- end }}
  # stage(aws): 위 값들은 ExternalSecret(SM/PS) + application-stage.yml 이 처리

  # K8s probe 그룹(/actuator/health/{liveness,readiness}) 노출 — liveness가 외부 의존성에 안 묶이게
  MANAGEMENT_ENDPOINT_HEALTH_PROBES_ENABLED: "true"
  SPRING_JPA_HIBERNATE_DDL_AUTO: {{ .Values.config.ddlAuto | quote }}
  SPRING_MAIL_HOST: "smtp.gmail.com"
  SPRING_MAIL_PORT: "587"
  SPRING_MAIL_USERNAME: {{ .Values.config.mailUsername | quote }}
  SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH: "true"
  SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE: "true"
  WALLET_API_BASE_URL: {{ .Values.config.walletApiBaseUrl | quote }}
  APP_PASSWORD_RESET_BASE_URL: {{ .Values.config.appPasswordResetBaseUrl | quote }}
  {{- if eq .Values.secretBackend "vault" }}
  # dev(vault): ES가 kafka를 안 주므로 configmap이 소유. stage(aws)는 ExternalSecret(PS) 단독 소유.
  KAFKA_BOOTSTRAP_SERVERS: {{ .Values.config.kafkaBootstrapServers | quote }}
  {{- end }}
