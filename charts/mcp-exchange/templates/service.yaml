apiVersion: v1
kind: Service
metadata:
  name: mcp-exchange
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: mcp-exchange
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
