apiVersion: v1
kind: Service
metadata:
  name: front-app
  labels:
    app: front-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: front-app
  ports:
  - name: http
    port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
