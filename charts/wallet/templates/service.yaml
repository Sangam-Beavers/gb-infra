apiVersion: v1
kind: Service
metadata:
  name: wallet
  labels:
    app: wallet
spec:
  type: {{ .Values.service.type }}
  selector:
    app: wallet
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
