apiVersion: v1
kind: Service
metadata:
  name: admin
  labels:
    app: admin
spec:
  type: {{ .Values.service.type }}
  selector:
    app: admin
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
