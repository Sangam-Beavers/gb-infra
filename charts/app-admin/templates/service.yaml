apiVersion: v1
kind: Service
metadata:
  name: app-admin
  labels:
    app: app-admin
spec:
  type: {{ .Values.service.type }}
  selector:
    app: app-admin
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
